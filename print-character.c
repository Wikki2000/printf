#include "main.h"

/**
 * print_character - Print character one at a time
 * @c: The character to be printed to the console
 *
 * This function print a character (c) one at a time
 *
 * Return: The count of character
 */
int print_character(char c)
{
	putchar(c);
	return (1);
}
